# Handover

## 2026-09-08 — GPT-5.6 Sol

### Where the problem now stands

Two cracking passes completed. The first froze the evidence as `. B ? ? W D .?` and killed the Balak-specific reading. The second ran the highest-value outward test and advanced the project to a formal solve claim.

**Claim submitted for validation:**

**`. B T D W D .` → `BTDWD` → “House of David.”**

Read first:

1. `analysis/2026-09-08-solve-case-btdwd.md`
2. `analysis/2026-09-08-branch-adjudication.md`
3. the solve-claim entry in `board/log/`

### Why the claim crossed the threshold

The load-bearing first-pass uncertainty was `T` versus `N` at position 2. Langlois' published image study contains an independent replication that resolves that fork substantially in favor of `T`: the same two recesses seen on the stone appear on the separate French Academy pre-break squeeze and, under RTI/specular enhancement, **cross and continue**. Meanwhile no `N` descender is visible below the fold.

The rest of the reconstruction stacks coherently:

- initial `B`: stone + squeeze, secure;
- `T`: replicated crossing morphology on stone + independent squeeze, now strongly preferred;
- first `D`: triangular squeeze evidence + RTI right-stroke angle, but still the largest material uncertainty;
- `W`: clear;
- final `D`: strong;
- post-`D` circular divider: strong in Langlois' RTI analysis, though Richelle/Burlingame dispute confirmation-level confidence;
- pre-`W` “sentence divider”: not original-stone evidence and probably derived from a hollow squeeze pseudo-feature / restoration;
- Balak's `LQ`: not observed.

The held-out Tel Dan `BYTDWD` dynastic parallel is then applied only after the glyph choice. It makes “House of David” substantially better grounded than `BNDWD`, an unattested `B+name-ending-WD`, or Balak.

### Strongest surviving objection

Richelle & Burlingame's critique is still the correct refuter lane: can the alleged first `D`, `T`, and terminal dot be reduced to irregular damage/folds strongly enough that the physical advantage disappears? The claimant's answer is no because the `T` crossing feature reproduces on an independent squeeze and because the proposed first `D` survives an RTI check designed specifically to separate hollow deformation from embossed incision. But validators should reproduce this rather than accept the prose.

### Validator falsifiers

Fail the claim if a validator can show any of:

1. the French Academy crossing recesses are misaligned/not the same sign position;
2. the crossing morphology fits another sign better than `T`, or secure taw comparanda contradict it;
3. all first-`D` evidence is squeeze deformation rather than letter morphology;
4. the terminal circular feature is materially unlike secure word dividers;
5. another complete reading fits the frozen physical marks at least as well **and** has an equal or stronger contemporary external anchor.

### Important scope limit

The solve claim is about the **most defensible line-31 reading**, not about proving every later biblical narrative concerning David. If the claim survives validation, what follows is that the best reconstruction contains a dynastic expression `BTDWD`; historical conclusions beyond that require a separate argument.

### Status

**SOLVE CLAIM — awaiting independent validators and human sign-off.** The claim file should be released from `board/active/`; do not update `STATUS.md` to “solved” before validation.
