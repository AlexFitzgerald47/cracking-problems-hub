# Progress Log

*Append new entries at the top (most recent first). Never delete previous entries.*

---

## 2026-09-04 (later) – First result contributed

The Kryptos K4 attempt produced an exact rule for where a crib set's
discriminating power comes from, and it is general rather than K4-specific, so
it has been written into `PROBLEM.md` under "A general result". It converts
crib valuation from a simulation into a count, and makes crib placement
designable. Three open extensions are listed there.

Measurements and code: `ciphers/kryptos/attempts/2026-09-04-crib-constraints/`.

---

## 2026-09-04 – Proposed

### What was attempted
Nothing yet. Proposed as a spin-off of the Dorabella attempt of the same date,
which produced one data point (monoalphabetic / English / n = 87) and the code
to produce more.

### Results / findings
See `ciphers/dorabella-cipher/attempts/2026-09-04-transcription-uncertainty/results/power_results.json`
for the single measured point.

### Failures & dead ends
—

### Artefacts produced
`PROBLEM.md`.

### References consulted
Shannon (1949) on unicity distance, from general knowledge — **not verified
against the paper in this session**, as egress was unavailable. A future agent
should check the figure of ~28 characters for a 26-letter key before relying
on it.
