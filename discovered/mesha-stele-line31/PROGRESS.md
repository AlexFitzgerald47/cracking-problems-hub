# Progress

## 2026-09-08 — GPT-5.6 Sol — first cracking pass

### What was attacked

Started from the published stone RTI, enhanced RTI, backlit Louvre squeeze, Louvre squeeze RTI, independent French Academy squeeze RTI, and the opposing mark-level tracings rather than from the proposed words. Current-status search found the dispute still live in 2026.

### Physical freeze

The defensible evidence-first sequence is:

**`. B ? ? W D .?`**

- initial `B`: strong;
- position 2: uncertain, `T` currently best but `N` remains possible;
- position 3: uncertain, `D` plausible but entangled with squeeze fold/wrinkle evidence;
- `W`: strong;
- final `D`: strong;
- divider after final `D`: probable but disputed;
- claimed sentence divider immediately before `W`: **not secure on original material** and cannot be treated as an observed boundary.

Full evidence ledger: `analysis/2026-09-08-branch-adjudication.md`.

### Main result

**The specific Balak (`BLQ`) proposal is rejected as an epigraphic decipherment.** Finkelstein, Na'aman & Römer do not observe `L` or `Q`; they explicitly have two erased positions and list several possible `B??` names before choosing Balak from biblical/historical context. Their branch also depends on a disputed pre-`W` sentence divider whose conspicuous full stroke is in reconstructed plaster and whose putative squeeze trace is not independently secure.

This does **not** eliminate every `B??` personal-name hypothesis. It eliminates the claim that the object itself reads or materially favors *Balak*.

### Aggressive but bounded reconstruction

With the unsupported split removed, the best search object is a single `B??WD` unit. `BTDWD` is the leading reconstruction because:

1. the two uncertain positions are at least compatible with `T` and `D` respectively;
2. there is no secure internal divider and a probable divider follows the last `D`;
3. the contemporary Tel Dan `BYTDWD` supplies an independent external dynastic parallel after the physical reading is frozen.

`BNDWD` and other single-unit `B??WD` forms remain live. Therefore **“House of David” is best reconstruction, not confirmed five-letter reading**.

### Failed / deliberately rejected shortcuts

- Did not treat the plaster-restored vertical line as ancient evidence.
- Did not accept backlit shadows automatically as incisions; squeeze folds and secondary deformation are a demonstrated confound.
- Did not use biblical geography to fill invisible consonants.
- Did not promote `BTDWD` to certainty merely because it has a strong lexical parallel.

### Outward tests preregistered

A future raw-RTI/3D pass should test, blind to lexical identity: crossing incision geometry at position 2; triangular channel geometry independent of the fold at position 3; original-incision morphology of any pre-`W` divider; and whether the post-`D` depression matches secure word dividers elsewhere on the stele.

### Status

**Material advance / branch collapse, not a solve claim.** The Balak-specific branch should be removed from the serious epigraphic candidate set; the recoverable frame is `B??WD`, with `BTDWD` currently first-ranked.
