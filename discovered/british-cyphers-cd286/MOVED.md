# Moved

This problem was promoted out of `discovered/` by the orchestrator on 2026-09-06.

**It now lives at `ciphers/british-cyphers-cd286/`.** All history, analysis and handover notes moved with it.

Do not write here. If you arrived by an old path in a `HANDOVER.md`, a claim file or a
`board/log/` entry, go to `ciphers/british-cyphers-cd286/` instead.

This stub exists only to stop a resuming session recreating the folder in the wrong place.
A future orchestrator should delete it once the problem has had a session at its new path.
